# LessonHub · 기능 & 컨셉 요약

> 이 문서는 **무엇을 하는 서비스인가(컨셉)**와 **어떤 기능이 있는가(기능)**만 담는다.
> 스택·디자인 토큰·화면 명세·구현 가이드는 [README.md](README.md) 참고.

---

## 0. 한 줄 컨셉

**개인 1:1 레슨 예약 SaaS.** 학생은 선생님의 가능 시간을 보고 예약하고, 선생님은 시간표·예약을 관리하며, 파워관리자는 사용자·연결·정책을 운영한다. (기본 시간대 `Asia/Seoul`)

---

## 1. 핵심 흐름

```
선생님이 가능시간 등록  →  학생이 담당 선생님 연결  →  빈 슬롯 보고 예약
        →  선생님이 승인/완료/노쇼 처리  →  수업 종료 시 자동 완료
```

- 학생 예약은 **담당 선생님 연결**이 전제 (연결돼야 그 선생의 슬롯을 본다)
- 예약 단위 시간은 **선생님 프로필의 레슨 길이(`lesson_duration_min`)** 기준으로 슬롯이 쪼개진다

---

## 2. 사용자 역할 & 기능

### 🎓 학생 (STUDENT)
- 담당 선생님 **검색 · 연결 · 해제**
- 선생님의 빈 슬롯 보고 **예약** / 본인 예약 **취소**
- **반복 예약** (매주 N회 정기 등록, 시리즈 일괄 취소 가능)
- 본인 정보 · 비밀번호 변경
- **계정 티어**: `FULL`(정식 가입) / `TEMP`(선생이 만든 임시 계정) → 학생이 **정식 전환(TEMP→FULL)**

### 👩‍🏫 선생님 (TEACHER)
- **가능시간 관리** 3종:
  - 주간 반복 시간표 (예: 월~금 09–18시)
  - 특정 날짜 일회용 시간 (예: 이번 주말 특강)
  - 예외/휴무 (특정일 휴강)
- **임시 학생 생성 · 연결** (학생이 계정 없어도 선생이 만들어 줌)
- 예약 **승인 · 완료 · 노쇼 · 취소** + 수업 코멘트
- **공개 프로필** (마케팅용 외부 URL `/t/<slug>`, 비로그인 접근 가능)

### 🛡️ 파워관리자 (POWER_ADMIN)
- 사용자 **CRUD** (생성/수정/소프트 삭제)
- 학생-선생 **강제 연결**
- 비밀번호 **강제 reset**
- **패치노트** 작성 (서비스 공지)
- **통계 · 활동 · audit log** 조회 / 정책 설정

---

## 3. 핵심 규칙 (컨셉상 중요한 것만)

| 규칙 | 내용 |
|---|---|
| **슬롯 계산** | 주간 시간표 + 일회용 시간 − 예외 − 기존 예약, 그리고 예약 가능 기간 내에서만 노출 |
| **자동 완료** | 수업 종료 시각이 지나면 `BOOKED → COMPLETED` 자동 전환 (매분 배치) |
| **노쇼** | 자동 아님 — 선생님이 **수동 처리**, 학생별 누적 카운트 |
| **취소 마감** | 학생: 수업 전날 특정 시각까지 / 선생: 수업 시작 N시간 전까지 |
| **중복 차단** | 같은 선생·같은 시작시각에 예약 1건만 (PENDING/BOOKED 기준) |
| **첫 로그인 강제 비번변경** | 임시 학생 생성·관리자 reset 시 다음 로그인에서 강제 변경 |
| **소프트 삭제** | 사용자 삭제는 하드 삭제가 아니라 비활성화(`is_active=FALSE`) + 연결 해제 |
| **마지막 관리자 보호** | 마지막 활성 파워관리자는 삭제 불가 |

---

## 4. 보조 기능

- **PWA** (아이폰 홈화면 설치)
- **Toast 인앱 알림** (성공/경고/에러)
- **Audit log** (관리자 액션 자동 기록)
- **Rate limit** (로그인/회원가입/계정복구 시도 제한)

---

## 5. 아직 없는 기능 (컨셉상 향후)

- **카카오 OAuth** (Supabase Auth 소셜 로그인으로 추후 연동)
- 그룹 수업(1:N)
- 학생 리뷰/평점
- 채팅/메시지
