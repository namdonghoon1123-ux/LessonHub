(() => {
  window.LH_RUNTIME_CONFIG = Object.freeze({
    // Keep debug helpers visible during active development.
    debugToolsEnabled: true,
    showQuickPresetButton: true,
    showHealthCheckButton: true,
    showTokenBox: true,
    showRequestLog: true,
    showTestAccountHints: true,
  });
})();
