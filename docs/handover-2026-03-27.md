# Handover - 2026-03-27

## 오늘 반영한 작업
- 사용자 제공 원본 로고 PNG를 프론트에 직접 적용
  - 원본: `/Volumes/Extreme_SSD/workspace/src/img/SCR-20260327-bbra.png`
  - 배포 자산: `/Volumes/Extreme_SSD/workspace/frontend/lessonhub-logo.png`
- 파비콘/애플 터치 아이콘 교체
  - `index.html`, `teacher-login.html`, `student-login.html`, `app.html`
  - `type="image/png"` + `/lessonhub-logo.png`로 통일
- 상단 네비게이션 정렬 보정
  - 링크/버튼의 세로 정렬 및 높이 통일(`align-items`, `min-height`, `line-height`) 적용
  - 대상 CSS:
    - `/Volumes/Extreme_SSD/workspace/frontend/lessonhub-auth.css`
    - `/Volumes/Extreme_SSD/workspace/frontend/lesson-booking-app.css`
- 프론트 빌드 자산 확장
  - Dockerfile에서 `*.png` 복사하도록 수정
  - `/Volumes/Extreme_SSD/workspace/frontend/Dockerfile`

## 검증
- `docker compose up --build -d frontend` 성공
- `docker compose ps`에서 `frontend/backend/db` 모두 `Up`
- 정적 페이지 응답 확인(HTTP 200)
  - `/index.html`
  - `/teacher-login.html`
  - `/student-login.html`
  - `/app.html`
  - `/lessonhub-logo.png`
- Playwright로 `/index.html`, `/teacher-login.html`, `/app.html` 화면 구조 확인

## 리스크/메모
- macOS 메타파일(`._*`)이 남아 있으면 Docker 빌드 에러 재발 가능
  - 필요 시 `find . -type f -name '._*' -delete && dot_clean -m .` 선제 수행

## 오늘 마무리 체크
1. 원본 PNG 로고 반영 완료
2. 상단 네비 정렬 보정 완료
3. 도커 재기동 및 동작 확인 완료
4. 커밋/푸시 진행 예정
